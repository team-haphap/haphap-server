# 코드 리뷰 이슈 초안 (2026-07-09)

각 이슈는 `.github/ISSUE_TEMPLATE/haphap_issue_template.md` 양식에 맞춰 작성했습니다. GitHub에 이슈 생성 시 그대로 복사해서 사용하시면 됩니다.

---

## 이슈 1

**title:** `[버그 수정] 카카오 로그인 시 이메일 null 허용으로 데이터 무결성 훼손`

### 🚀 이슈 유형

- [ ] 기능 추가
- [x] 버그 수정
- [ ] 리팩토링
- [ ] 기타 (설명 추가)

### ✨ 기능

카카오 로그인 시 이메일이 없는 계정도 그대로 가입/저장되는 문제를 막고, 이메일 미동의 사용자에 대한 처리 방식을 명확히 한다.

### 📒 작업 상세 내용

- [ ] `KakaoOAuthClient.java:64` — `getUserInfo()`에서 `account.email() == null`인 경우 처리 분기 추가
- [ ] 신규 에러코드 `EMAIL_REQUIRED`(가칭) 정의 후 `CustomException`으로 던지거나, 카카오 인가 요청 시 이메일 동의항목을 필수 scope로 강제하는 방식 중 택1 결정
- [ ] (필요 시) `User.java:29` `email` 필드에 `nullable = false` 제약 추가
- [ ] `UserService.java:38`에 null 체크 로직 보강
- [ ] 관련 예외/정책 테스트 코드 작성

### 📎 참고 자료

- `KakaoOAuthClient.java:64`, `User.java:29`, `UserService.java:38`
- 카카오 이메일 동의항목은 선택 동의이므로 응답에 없을 수 있음

---

## 이슈 2

**title:** `[버그 수정] PopularPostingService 인기 공고 노출 개수 오류 (8 → 4)`

### 🚀 이슈 유형

- [ ] 기능 추가
- [x] 버그 수정
- [ ] 리팩토링
- [ ] 기타 (설명 추가)

### ✨ 기능

`PopularPostingService`의 인기 공고 반환 개수가 명세(4개)와 다르게 8개로 설정된 문제를 수정한다.

### 📒 작업 상세 내용

- [ ] `PopularPostingService.java:24` `MAX_POPULAR = 8` → `4`로 수정
- [ ] `.limit(MAX_POPULAR)` (60행) 동작 확인
- [ ] 검색 페이지용 `PopularSearchPostingQueryService`의 `POPULAR_COUNT = 4`와 값 일치 여부 재확인
- [ ] 관련 테스트/응답 개수 검증

### 📎 참고 자료

- `PopularPostingService.java:24`
- 비교 대상: `PopularSearchPostingQueryService` (`POPULAR_COUNT = 4`, 정상)

---

## 이슈 3

**title:** `[버그 수정] 공고 검색 status 파라미터 미검증으로 잘못된 값이 마감 처리됨`

### 🚀 이슈 유형

- [ ] 기능 추가
- [x] 버그 수정
- [ ] 리팩토링
- [ ] 기타 (설명 추가)

### ✨ 기능

공고 검색 시 `status` 값이 `open`이 아닌 임의의 문자열("abc" 등)이어도 예외 없이 마감 공고로 조용히 처리되는 문제를 막고, 잘못된 값 입력 시 명시적으로 에러를 반환하도록 한다.

### 📒 작업 상세 내용

- [ ] `PostingSearchQueryService.java:56` `matchesStatus()` 로직 검토 (`status.equals("open") == isOpen` 비교 방식이 원인)
- [ ] 컨트롤러 단 DTO 검증(`@Pattern`) 또는 `PostingSearchCondition` 생성 시점 검증 중 방식 택1
- [ ] `status`가 `open`/`closed`/`null` 외의 값이면 `CustomException`(예: `INVALID_STATUS`) 던지도록 추가
- [ ] (근본적 해결) `status`를 `String` 대신 `enum(PostingStatus)`으로 타입 변경 검토
- [ ] 잘못된 status 값 입력 시 에러 응답 테스트 추가

### 📎 참고 자료

- `PostingSearchQueryService.java:56`
