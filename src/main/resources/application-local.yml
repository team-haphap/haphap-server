spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/haphap
    username: haphap
    password: 비밀번호
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create